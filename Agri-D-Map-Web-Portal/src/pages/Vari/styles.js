import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  grid_container: {
    padding: "50px",
  },
  segmented_image: {
    height: "300px",
    width: "300px",
  },
  ind_grid: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
  },
}));
